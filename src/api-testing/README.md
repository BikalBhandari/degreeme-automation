# API Testing

Planned context for validating DegreeMe API endpoints directly (without browser).

## Planned Scope

- Quiz configuration API responses
- Degree recommendation API (the AI endpoint that generates results)
- Response schema validation
- Error handling and edge cases

## Status

Not yet implemented. Tests live in `tests/`, specs in `specs/`.
